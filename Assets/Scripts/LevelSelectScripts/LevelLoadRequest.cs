using UnityEngine;

// holds the selected level name when changing scenes for level select scene
    public static class LevelLoadRequest
    {
        public static string RequestedLevelName;
        public static bool IsCustomLevel = false;
    }